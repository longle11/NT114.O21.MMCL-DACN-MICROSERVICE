name: test-issue

on:
  pull_request:
    paths:
      - "Issue/**"

jobs:
  build:  
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd Issue && npm install && npm run test:ci