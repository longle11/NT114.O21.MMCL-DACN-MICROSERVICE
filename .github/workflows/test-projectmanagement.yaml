name: test-projectmanagement

on:
  pull_request:
    paths: 
      - "ProjectManagement/**"
jobs:
  build:  
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd ProjectManagement && npm install && npm run test:ci